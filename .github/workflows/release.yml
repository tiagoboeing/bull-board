name: Create release

on:
  workflow_dispatch:
    inputs:
      version:
        description: "Type of version to release"
        type: choice
        required: true
        options:
          - major
          - minor
          - patch

concurrency: release-${{ inputs.version }}

jobs:
  create-release:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Extract branch name
        run: echo "BRANCH_NAME=$(echo ${GITHUB_REF#refs/heads/} | tr / -)" >> $GITHUB_ENV

      - name: Release It
        id: release
        uses: actions/setup-node@v3
        with:
          node-version: 16
        run: |
          npm ci
          npm run release -- --dry-run ${{ inputs.version }}
          echo "version=$GENERATED_VERSION" >>$GITHUB_OUTPUT

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push Docker images
        uses: docker/build-push-action@v3.2.0
        with:
          push: true
          tags: tiagoboeing/bull-board:${{ steps.release.outputs.version }}
          target: production
